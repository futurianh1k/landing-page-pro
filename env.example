# 환경 변수 설정 템플릿
# 
# 사용법:
# 1. 이 파일을 복사하여 .env.local (개발), .env.staging, .env.production 파일 생성
# 2. 각 환경에 맞는 값으로 수정
#
# 예시:
#   cp env.example .env.local
#   # .env.local 파일 편집
#
# Vite 환경 변수 규칙:
# - VITE_ 접두사가 붙은 변수만 클라이언트 코드에서 접근 가능
# - import.meta.env.VITE_변수명 으로 접근

# ============================================
# 환경 식별
# ============================================
# development | staging | production
VITE_APP_ENV=development

# ============================================
# Azure Functions API
# ============================================
# 개발: http://localhost:7071
# 스테이징: https://func-landing-page-pro-staging.azurewebsites.net
# 프로덕션: https://func-landing-page-pro.azurewebsites.net
VITE_AZURE_FUNCTIONS_URL=http://localhost:7071

# ============================================
# Microsoft Entra External ID (인증)
# ============================================
# Azure Portal > Entra External ID > 앱 등록에서 확인
VITE_ENTRA_CLIENT_ID=your-client-id
VITE_ENTRA_TENANT_ID=your-tenant-id
VITE_ENTRA_AUTHORITY=https://your-tenant.ciamlogin.com

# ============================================
# 디버그 설정
# ============================================
# true | false
VITE_DEBUG=true

# debug | info | warn | error
VITE_LOG_LEVEL=debug

# ============================================
# API 설정
# ============================================
# API 타임아웃 (밀리초)
VITE_API_TIMEOUT=30000

# ============================================
# Application Insights (모니터링)
# ============================================
# Azure Portal > Application Insights > 연결 문자열에서 복사
# 프로덕션: InstrumentationKey=xxx;IngestionEndpoint=https://koreacentral-0.in.applicationinsights.azure.com/;...
VITE_APPINSIGHTS_CONNECTION_STRING=

# ============================================
# 앱 버전 (선택)
# ============================================
VITE_APP_VERSION=1.0.0

# ============================================
# Google Analytics (선택)
# ============================================
# VITE_GA_MEASUREMENT_ID=

# ============================================
# Azure Functions 백엔드 환경 변수 (참고용)
# ============================================
# 아래 변수들은 Azure Functions의 local.settings.json에 설정됩니다.
# 프론트엔드에서는 직접 사용하지 않지만, 참고용으로 기록합니다.

# 웹 검색 API Keys (Azure Functions에서 사용)
# TAVILY_API_KEY=your-tavily-api-key
# SERPER_API_KEY=your-serper-api-key

# AI API Keys (Azure Functions에서 사용)
# GEMINI_API_KEY=your-gemini-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# OPENAI_API_KEY=your-openai-api-key

# Vertex AI / Imagen API (이미지 생성용, Azure Functions에서 사용)
# VERTEX_API_KEY 또는 GOOGLE_APPLICATION_CREDENTIALS (서비스 계정 키 JSON 파일 경로)
# VERTEX_PROJECT_ID=your-google-cloud-project-id
# VERTEX_LOCATION=us-central1
